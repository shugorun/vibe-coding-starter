issue を1件1ファイル（`NNNN-スラッグ.md`、NNNN は既存最大値 +1 の連番）でこの階層に直接置く。状態は frontmatter `status: open|resolved` で持ち、ディレクトリ移動はしない。本文の節は種別で選ぶ:

- **問題**（コード / 設定を直せば消えるもの）: 現象 / 再現 / 試した対処 / 解決
- **実装スライス**（計画・spec を分解した作業単位。frontmatter に `type: HITL|AFK` を追加 — HITL = 人の判断・確認（設計判断・デザインレビュー等）が必要、AFK = 人なしで実装・完了できる）: 作るもの（端から端までの振る舞い。ファイルパス・コード片は書かない — 例外: プロトタイプが決定を担うスニペット（状態機械・スキーマ等の形）を生んだ場合のみ引用可） / 受け入れ条件（チェックリスト） / ブロック（先に完了すべき issue のファイル名。なければ「なし」）
