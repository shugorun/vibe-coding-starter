# 2026-W21

## 2026-05-23

### やったこと

- ボイラープレートのレビュー指摘を一括反映（公開前整備）。
- フックを PowerShell `.ps1` → クロスプラットフォーム Node `.cjs` に置換（Windows / macOS / Linux 対応）。`session-start.cjs` / `stop.cjs` を新規作成、旧 `.ps1` を削除。実機で両フックの動作を確認（緑）。
- `stop` フックを `systemMessage`(JSON) 出力に変更。従来の `Write-Output` + exit 0 は Stop フックでは transcript モードでしか出ず、通常ビューに出ていなかったため。
- `settings.json`: hook コマンドを `node ...` に更新／効いていなかった脆い週フォーマット allow を削除／`Bash(date:*)` 追加／secret の deny を Read・Edit・Write で対称化。
- `LICENSE`(MIT) を追加。
- `README.md` に「このテンプレから始める」節（Use this template / clone+reinit / フック信頼承認 / `acceptEdits` 既定の注記）を追加。OS 前提を Win/mac/Linux に更新。
- 週番号取得を OS 別に明記（Unix: `date +%G-W%V`、Windows PS: `Get-Date` 式）。checkpoint / wrapup / 00-INDEX を更新。
- commit prefix 表を `02-GUIDELINES.md` に一本化し、checkpoint スキルからは参照に変更（二重管理の解消）。透明性文言の "PowerShell" を "シェル" に中立化（CLAUDE.md / 02-GUIDELINES）。

### 決めたこと

- Mac 対応はフックの Node 化で実現する。理由: catchup 初期化方式や settings の OS 分岐より手間が最小で、Node は Claude Code 自体が必ず要求するので追加依存にならない。
- secret の deny は `.env.*` 全体ではなく `.env` / `.env.local` / `.env.*.local` に限定する。理由: `.env.example`（`.gitignore` で `!` 除外＝コミット対象のテンプレ）を編集可能に保つため。`.env.production` 等に secret を置く運用なら各プロジェクトで deny を追加する。
- `Read(//**)` は意図（どこでも読める）が効いている可能性があり、誤って狭めるリスクを避けて据え置いた。
- LICENSE は MIT、著作権者は暫定で `shugorun`。

### 次やること

- `LICENSE` の著作権者名を確定する（必要なら本名へ）。
- 公開前に英語 README の要否を判断する（現状 JP のみ・想定読者次第）。
- 新規プロジェクトで Node フックの信頼承認フローを実機確認する。

## 2026-05-23（追記: stop フック判定ロジックの見直し）

### やったこと

- `stop.cjs` の発火条件を「dirty かつ progress ログが存在する」→「dirty かつ **今日の progress エントリが無い**」に変更し、`stop_hook_active` ループガードを追加（app3 で先に育っていた版を逆輸入）。実行確認 OK（今日は記録済みのため正しく沈黙）。

### 決めたこと

- stop フックは today 判定方式を採用する。理由: **Stop フックは応答ごと（毎ターン）に発火**し `systemMessage` は重複抑制が無いため、「dirty のみ」だと開発中ほぼ全ターンで通知が出てノイズ化する。「今日まだ未記録」を条件にすると、checkpoint 規律を破っている間だけ鳴り、最初の checkpoint で自動的に沈黙する（信号に紐付き・コンプライ即停止）。
- session 単位の1回限り抑制は**入れない**。バックストップは守るまで促し続け、守った瞬間止まるのが正しく、1回で諦めると記録漏れの日を見逃すため。
- 「dirty のまま終了」の検知は Stop フックの責務にしない。Stop はセッション末を判別できない（毎ターン発火）ため、それは `/wrapup-project`（明示）と `/catchup-project`（dirty start 復元）が担う。

### 次やること

- （変更なし）公開前の英語 README 要否判断、LICENSE 名義確定。

## 2026-05-25

### やったこと

- コーディング規則の curated doc `docs/08-CODING.md` を新規作成。
- 実装時に必ず読ませる導線を 5 か所に追加: `CLAUDE.md`（「コードを書くとき」節 + 入口リンク）/ `00-INDEX.md` 必読順 / `02-GUIDELINES.md` 実装ルール先頭 / `catchup-project` 実装文脈チェック / `checkpoint` 検証節の lint ゲート参照。

### 決めたこと

- コーディング規則は「機械的に強制できる規則は lint / formatter / typecheck に委譲、prose には判断を要する規則だけ」を背骨にする。理由: スタイルを prose で書いても破られ得るが、ツール設定は破られない。curated doc も薄く保てる。
- 段階運用をスタック表と揃える: 探索/MVP 期は prose を手動順守、pin 後はスタイル系をツールへ寄せ、ツール設定と prose が矛盾したらツールを正とする。
- Claude のデフォルト（コメント最小・YAGNI・境界バリデーション・secret/injection 対策等）は再掲せず、薄いポインタ列挙だけ残す。理由: 重複はノイズで全体の効きを薄める。doc の自己完結性は「再説明せずポインタで列挙」で両立。
- 数値（関数40行 / ファイル300行 / 引数4個 / ネスト3段）は全て「目安＝分割検討の閾値」とし、厳格な上限にしない。理由: 厳格化すると無意味な分割を誘発する。
- 言語固有ルール（`any` 禁止・型注釈必須・docstring 必須等）と具体ツール名は、スタック未確定のため今は書かず pin 後に lint/tsconfig へ委譲する方針だけ記載。

### 次やること

- スタック pin 後、`08-CODING.md` の「検証（lint ゲート）」節に実際の lint / formatter / typecheck 設定ファイルへのリンクを追記する。
- （変更なし）公開前の英語 README 要否判断、LICENSE 名義確定。

## 2026-05-25（追記: 08-CODING を agent 向けに書き直し + ツール自動導入ルール）

### やったこと

- `08-CODING.md` を「設計者が方針を語る」文体から「実装するエージェントへの命令形」に全面リライト。編集メタ（`薄く保つため` / `ここで再掲しない` / `二重管理しない` 等）を削除し、命令＋短い理由に統一。
- 「最初にコードを書くときにツールを入れる」節を新設。CLAUDE.md「コードを書くとき」も旧「pin 後に」から新モデルへ更新。

### 決めたこと

- **lint/formatter/typecheck の導入は「pin 後」に先送りせず、新しい言語で最初にコードを書くときに自動で行う。** ユーザに聞かず（引き返せる決定）、選定を progress に1行記録。理由: 「すぐ書き始めて質問しない」運用だと先送り先（pin）に永遠に到達せず lint ゲートが空転する。初回導入なら commit 前ゲートが初回から実弾になる。
- ツール選定はエコシステム定番をデフォルト構成で採る（TS→ESLint+Prettier+tsc / Python→Ruff+型 / Go→gofmt+vet / Rust→rustfmt+clippy）。使い捨て PoC は省略可。
- Claude の既定（コメント最小・YAGNI・境界検証・secret/injection 回避等）は1段落のポインタに圧縮し、再説明しない。理由: agent 向け doc では既定の再掲はノイズ。

### 次やること

- （変更なし）スタック確定時に lint 設定リンクを追記、公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: app-design でプラットフォーム & 言語を決める導線）

### やったこと

- `app-design/technical.md` に「プラットフォーム & 言語」セクションを新設（アプリ種別 → 言語 → スタックの順、決定表付き）。
- 導線を 3 か所に追加: `app-design/_index.md` 運用 / `app-design/_TEMPLATE.md` technical テンプレ / `02-GUIDELINES.md` ドキュメント分割。

### 決めたこと

- **app-design フェーズに入ったら最初に「アプリ種別（Web / モバイル / デスクトップ / CLI / その他）→ 言語 → スタック」を決める。** これは一方通行決定として扱い、選択肢とトレードオフを提示してユーザ確認を取る。理由: 種別と言語は後から変える高コスト決定であり、ここで確定すれば 08-CODING のツール自動導入が「確定済み言語」に対して動く＝前ターンの「暗黙の言語選択」論点が閉じる。
- 確定した種別は `01-PRODUCT.md`「対象プラットフォーム」へ、スタック詳細は M3 で stack 表に pin する流れは従来どおり。

### 次やること

- （変更なし）スタック確定時に lint 設定リンクを追記、公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: フェーズ体系を6段に再設計＋ドキュメント整理）

### やったこと

- フェーズを P0-P6 に再定義し、各フェーズに「移行ゲート（チェックリスト）」を持たせた（`03-ROADMAP.md`）。`05-PROGRESS.md` を新フェーズ＋現フェーズゲート表示に刷新。
- `CLAUDE.md` を短い契約に圧縮（フェーズ / `/work` ループ / 記録判断 / 入口のみ）。運用詳細は `02-GUIDELINES.md` に一本化し、CLAUDE・README・各スキルからの再掲を排除。
- `docs/09-ENVIRONMENT.md` を新設（OS・シェル・ツールチェーン版・既知の罠）。実装時に読む導線を 08-CODING / 00-INDEX / catchup に追加。
- `04-ARCHITECTURE.md` にディレクトリ方針（`mvp/` 使い捨て・`app/` 本実装0から）を明記。
- `requirements/` フォルダを廃止し、PRD/要件は app-design + specs に統合。全 doc の `requirement` 参照を app-design/spec に置換。
- doc 全体を「設計を語る」文体から Claude 向けの直接的記述へ寄せ、冗長を削減。

### 決めたこと

- フェーズは MVP(P2) を app-design(P3) の前に置く。理由: 最小実装で中核体験（コンテンツ）を先に確定し、確定後に詳細設計 → 本実装する。MVP は使い捨てで本実装(P5)は `app/` で 0 から書く（適当に書いた MVP コードを本実装に持ち込まない）。
- ドキュメントの単一の正は `02-GUIDELINES.md`、`CLAUDE.md` は常時ロードの最小契約に限定。理由: 同一ルールが複数 doc に重複していたのがコンテキスト肥大と齟齬の主因。概念ごとに正を1つに固定しポインタ参照にする。
- `requirements/` 廃止（ユーザ確認済）。MVP + app-design + spec で要件を担う。
- 環境情報は専用 doc に分離（ユーザ要望）。環境起因の失敗を踏むたび追記する運用にする。

### 次やること

- （このセッション内）`/work` スキルと skills/hooks 更新を別コミットで入れる。
- （継続）スタック確定時に `08-CODING.md` へ lint 設定リンク追記、公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: /work スキル新設とスキル/フックの追従）

### やったこと

- 新スキル `/work` を新設（`.claude/skills/work/SKILL.md`）。自然文の作業依頼に対し ①確認（ユーザ側タスク含む）→ ②コーディング（issue 起票）→ ③docs更新（ADR 起票）→ ④commit を順に実行。各ステップで該当規則 doc を読む導線にした。
- `/checkpoint` を `/work` の④として位置づけ（流用）、curated doc リストから requirements を除去。
- `/catchup-project` を新フェーズ・ゲート・`mvp`/`app`・`09-ENVIRONMENT` 対応に更新。サマリ後の作業は `/work` へ接続。
- `/wrapup-project` をフェーズ移行ゲート更新・requirements index 除去・M3→P3 に追従。
- `session-start.cjs` の案内に `/work` とフェーズ/ゲート行を追加（`node` 実行で出力確認 OK）。
- `mvp/README.md` を新設（使い捨て・本実装に引き継がない旨）。

### 決めたこと

- issue/ADR の起票漏れは `/work` の②③で「その場で起票」を厳守事項にして防ぐ。ユーザ側タスクの放置による失敗は①で着手前に洗い出して防ぐ。理由: ユーザが挙げた主要な失敗モード（起票漏れ・ユーザタスク保留）に直接対応する導線をスキルに埋める。
- スキルは catchup / work / checkpoint / wrapup の4つ。checkpoint は単独でも残す（「コミットだけ」を呼べるように）。理由: 覚える数を増やさず commit 単独実行の口も保つ。

### 次やること

- 実プロジェクトで `/work` フローと新フェーズゲートを通しで試し、文言を調整する。
- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: ゲート設計の原則 + 言語決定を P1 へ前倒し）

### やったこと

- ゲート設計の原則を `03-ROADMAP.md` に明記: **各ゲートは「次フェーズを始めるのに最低限必要な決断・入力」を含む**（「終わった証明」でなく「次に進む準備の証明」）。`CLAUDE.md` / `02-GUIDELINES.md` にも要約を反映。
- **アプリ種別・言語/主要フレームワークの決定を P3(app-design) → P1 のゲートに前倒し**（MVP を作るには言語が必須なため）。`03-ROADMAP` の P1/P3 ゲートと `05-PROGRESS` の現ゲートを更新。
- `04-ARCHITECTURE.md` のスタック表を 2 段階 pin に再構成（P1: アプリ種別・言語/ランタイム / P3: 残りの行）。「アプリ種別」「言語 / ランタイム」行を追加。
- `app-design/technical.md` / `_TEMPLATE.md` / `_index.md` の「プラットフォーム & 言語」を「P1 で確定済み・ここでは残りを詰める」に再フレーム。
- `/work` step 0 に「P2 に入るには P1 ゲート（種別・言語）が確定済みであること」を追加。

### 決めたこと

- ゲートは前方視点（次フェーズに必要な入力）で設計する（ユーザ指摘）。理由: 後方視点（終わった証明）だとゲート項目が恣意的になり、必要な前提決定の抜けを防げない。前方視点なら「何が足りなくて次に進めないか」が一意に決まる。
- 言語・アプリ種別は MVP の前（P1）に user-confirmed で pin する。MVP と本実装は言語を共有し、MVP が言語不適を示した場合のみ P3 で ADR 見直し。理由: MVP の目的はコンテンツ確定でありツール検証ではないので、言語は先に固めてよい。
- スタック pin は 2 段階（P1: 種別・言語、P3: 残り）。理由: MVP に必要な決定だけ先に固め、DB/認証/ホスティング等は MVP の学びを踏まえ後で固める方が手戻りが少ない。

### 次やること

- 実プロジェクトで P1 ゲート（言語確定）→ P2 MVP の流れを試し、言語の提示のしかたを調整する。
- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: MVP と本実装でスタックを分離・引き継がない）

### やったこと

- 直前の「言語を P1 で確定し本実装まで共有／スタック 2 段階 pin」を撤回し、**MVP(P2) と本実装(P5) のスタック判断を完全独立**に変更。
- P1 ゲート: 「MVP を最小コストで作る手段（種別・言語 / FW）を決める」に変更（使い捨て・pin しない・本実装と別でよい）。
- P3 ゲート: 「本実装の種別・言語・スタックを 0 から確定し `04-ARCHITECTURE` を pin」に変更。
- `04-ARCHITECTURE` のスタック表を「本実装専用・P3 で pin」に戻し、2 段階 pin と「P1 で pin」注記を削除（種別・言語 / ランタイム行は本実装用として残置）。
- `CLAUDE` / `02-GUIDELINES` / `05-PROGRESS` / `app-design`（_index / technical / _TEMPLATE）/ `/work` step0 / `mvp/README` を同方針に追従。

### 決めたこと

- **MVP と本実装でスタックは引き継がない**（ユーザ指摘）。理由: 本番がモバイルでも MVP は重いネイティブを避けてウェブで安く中核体験を検証する、のような判断ができる。MVP の目的はコンテンツ検証で技術は最小コストで選ぶ。本実装の種別・言語・スタックは P3 でコンテンツの学びだけを入力に 0 から決める。
- MVP のスタックは pin しない（使い捨て＝引き返せる）。記録は progress + `mvp/README.md` の 1 行。本実装のスタックだけ `04-ARCHITECTURE` で pin（一方通行）。
- 本変更は同日の「スタック 2 段階 pin」決定を supersede する（履歴の該当エントリは記録として残置）。

### 次やること

- 実プロジェクトで P1（MVP の作り方）→ P2 → P3（本実装スタックを 0 から）の流れを試し、提示のしかたを調整する。
- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: スキル使い分けを階層表記に修正）

### やったこと

- `02-GUIDELINES`「作業フロー」表が catchup / work / checkpoint / wrapup を一直線に並べ、一単位の連続フローに見える誤解を生んでいたため「スキルの使い分け」に改題し、3 階層（セッションを挟む catchup/wrapup・依頼ごとの work・work の④の checkpoint）を図で表記。

### 決めたこと

- 4 スキルは一直線フローでなく階層関係（catchup/wrapup がセッションを挟む ⊃ work を何度も ⊃ work の④ = checkpoint）として示す。理由: フラットな表だと catchup が work 内で動くかのように読め、scope の違い（セッション / 依頼 / サブステップ）が潰れる（ユーザ指摘）。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: /work を固定4ステップ→「確認ゲート＋不変条件ループ」に抽象化）

### やったこと

- `/work` を「確認 → コーディング → docs更新 → commit の固定4ステップ」から「**着手前の確認（固定点）→ 依頼を作業単位に分解 → 各単位を不変条件付きで進める作業ループ → checkpoint（固定点）**」に作り直し（`.claude/skills/work/SKILL.md`）。
- 不変条件（随時発火）= 触る前に規則を読む / 罠→issue / 判断→ADR。順序ではなく作業中つねに守るものとして再定義。
- `CLAUDE.md` の /work 節、`02-GUIDELINES`「スキルの使い分け」の図と説明、`README` / `00-INDEX` / `catchup` の gloss、`session-start.cjs` の案内を新モデルに追従。

### 決めたこと

- /work は 1 スキルのまま、中身を「固定点 + ループ」に抽象化する（プリミティブ分割はしない）。理由: (1) 現実の作業は非線形（戻る / 割り込む / 抜ける / commit 複数回）で固定4ステップは不正確。(2) issue/ADR 起票の強制力は「独立スキルとして存在」ではなく「/work が必ず通る不変条件」から来る。分割すると起票漏れ（元の問題）が再発する。(3) 少スキルの利点を保つ。checkpoint は唯一分けるべきプリミティブで既に独立済み。
- 「仕事を分解して割り振る」直感は、新スキルではなく /work 内の「作業単位に分解」ステップとして取り込む（1 単位 = 1 コミット = 1 checkpoint）。

### 次やること

- 実プロジェクトで /work の作業ループ（分解 → 各単位 checkpoint）を回し、単位の切り方の粒度を調整する。
- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: docs/mvp-design/ を新設・MVP の実験記録と可逆性規律）

### やったこと

- `docs/mvp-design/` を新設（P2 MVP の実験記録の作業場）。6 ファイル: `_index` / `scope`（可変スコープ）/ `experiments`（実験ログ）/ `switches`（切替変種の台帳）/ `feedback`（ユーザ反応）/ `problems`（困りごと）。
- 配線: `00-INDEX`（成長フォルダ）/ `02-GUIDELINES`（記録ルール表に生ログ行を追加）/ `03-ROADMAP` P2（記録先 + 可逆性規律）/ `/work` step3（P2 の記録・切替規律）/ `catchup`（P2 で _index を読む）/ `mvp/README` / `README` 流れ図。

### 決めたこと

- MVP の実験記録は docs 内（`docs/mvp-design/`、名前に mvp）に置く（ユーザ判断: 知識を docs 外に散らさない）。生ログはここ、確定した学び・コンテンツは `app-design` / `progress` に昇格（二重持ちしない）。
- 1 ファイルに混ぜず分割（ユーザ指示）。スコープ可変 / 実験ログ / 切替管理 / フィードバック / 問題。`switches`（台帳＝状態）と `experiments`（履歴）は形が違うので別ファイル。
- 可逆性は「全変更を可逆」ではなく「**比較・切り戻したい実験変種を切替可能に保ち、動いていた状態を壊さない**」に限定（ユーザ選択）。機構はパラメータ化でも並置でも可。`switches.md` が台帳。
- problems は MVP 中の未整理の困りごと。再現可能で確定した不具合は既存 `docs/issues/` に昇格（重複回避）。

### 次やること

- 実 MVP で mvp-design の各ファイルのテンプレ・粒度を使ってみて調整する。
- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: issue / environment の定義を明確化、「罠」依存の表現を排除）

### やったこと

- `docs/issues/_index.md` と `docs/09-ENVIRONMENT.md` の冒頭に「定義」節を追加。issue = このプロジェクトの解決すべき問題（直せば消える・Open→Resolved で追跡）/ environment = クライアント環境の恒久的な事実と環境起因の制約・回避策（直しても消えない・閉じない）。境界テスト「コード/設定を直せば消えるか?」→ Yes は issue。
- 「罠」依存の起票トリガーを定義に沿った表現へ全置換（CLAUDE / 02-GUIDELINES 記録表+checkpoint境界 / README / 00-INDEX / 08-CODING / catchup / checkpoint / work / mvp-design/problems）。「罠→issue」→「問題→issue」、「環境の罠」→「環境の制約・回避策」、09-ENVIRONMENT 見出し「既知の罠」→「既知の制約と回避策」。
- issue テンプレ「再現手順」→「再現手順（あれば）」（再現性は必須でないと明示）。

### 決めたこと

- issue / environment を曖昧語（「罠」）でなく**操作的な定義と境界テスト**で規定する（ユーザ指摘）。理由: 「罠」は標準化された概念でなく各セッションで線引きがぶれ、起票漏れ（元の主要失敗）を再生産する。語でなくテストで書けば判断が一意になる。
- 「再現する」は issue の定義軸にしない（ユーザ指摘: 再現性は付随的）。issue の本質は「解決すべき・直せば消える・追跡して閉じる」問題であること。
- 困りごとは 3 分岐: 未整理＝`mvp-design/problems`（低い敷居）/ 解決すべき確定問題＝`issues` / 直らない環境制約＝`09-ENVIRONMENT`。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: /work step3 の不変条件リストを整理）

### やったこと

- `/work`「3. 作業ループ」の不変条件リストを整理（重複していた「コード前に読む / docs 編集前に読む」を 1 行に統合、環境制約の記録を issue 行から独立、並びを「触る前に読む・従う → 起きたら残す → P2」に）。
- スタック逸脱のトリガーを「P5 以降」→「pin 後」に修正（pin は P3 ゲート。phase 番号でなく pin 後が正しいトリガー）。
- 確認節を「前提条件が未解決のまま着手しない」に明確化。

### 決めたこと

- /work step3 は「触る前（規則・スタック）」系と「起きたら残す（問題→issue / 環境制約→09-ENVIRONMENT / 判断→ADR / 仕様同期）」系の 2 系統＋P2 で並べる。理由: 元のフラット列が読む系と起票系を混在させ読みにくかった。系統で並べると抜け漏れ確認が一意になる。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: テストの置き場所と再利用を定義）

### やったこと

- `06-VALIDATION.md` に「テストの置き場所と再利用」節を新設。`04-ARCHITECTURE.md` ディレクトリ方針と `08-CODING.md` テスト節に置き場所ポインタを追記。

### 決めたこと

- テストはコードと同居（`docs/` 配下でない）。本実装は `app/`、MVP は `mvp/`。具体レイアウトは言語・スタックの慣習に従い P3 で確定し `04-ARCHITECTURE` に記す（ギャップだった「置き場所がどこにも書いてない」を解消）。
- **本実装(P5)のテストは残して再利用**（回帰スイート・checkpoint で回す）。**MVP(P2)のテストは使い捨て**で `app/` に引き継がない（普通ほぼ書かない）。RAG の評価スクリプト等は `mvp/` に置き結果は `mvp-design/experiments.md` へ。理由: 「再利用しない前提か」の問い（ユーザ）に対し、MVP は捨てる/本実装は残す、で MVP 使い捨て原則と一貫させた。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: 「pin」を言語化して正式定義）

### やったこと

- `02-GUIDELINES`「status 定義」に **pin の正式定義**を追加（`🔍 reviewed` の隣）。`04-ARCHITECTURE` の pin 文言をそれに合わせ、定義は GUIDELINES を指す形に。

### 決めたこと

- pin は precise な語（操作が1点に定まる）なので、罠と違い**消さず1回だけ正式定義して各所はそれに乗せる**（ユーザ選択 = (a)）。定義: 「`04-ARCHITECTURE` スタック表の行をユーザ承認で `🔍 reviewed` にして確定・固定する操作（P3 ゲート）。以後その行が唯一の正、逸脱は ADR 先行。MVP の使い捨てスタックは pin しない」。理由: 定義が 04-ARCHITECTURE に1回あるだけで他 8 箇所は定義なし使用 → 04 を読んでない AI がひっかかる。catchup で必ず読む GUIDELINES status 節に置けば全用例が解決する。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: PoC を一級市民化・poc/ 追加と [mvp]/[poc] prefix 分離）

### やったこと

- `poc/` フォルダを新設（使い捨て技術スパイクの作業場・any phase・`app/` を汚さない隔離箱・結論は research / spec へ promote）。`poc/README.md` 追加。
- 3 作業場モデルに整理（`mvp/`=製品検証・P2 / `poc/`=技術検証・any phase / `app/`=本実装・永続）。`04-ARCHITECTURE` ディレクトリ方針・`00-INDEX`・`README`・`CLAUDE`・`mvp/README` に反映。
- commit prefix 整理: **`[mvp]` 新設**（P2 の使い捨て作業 = `mvp/` + `mvp-design/`）、**`[poc]` を「技術検証スパイク（`poc/`・any phase）」に修正**（MVP を外す）。`checkpoint` の prefix 一覧にも `[mvp]`。
- `03-ROADMAP` 優先順位 / `08-CODING` / `06-VALIDATION` の MVP・PoC 表記を整理。

### 決めたこと

- PoC は research に畳まず一級市民として扱う（ユーザ指摘）。理由: PoC は早期だけでなくライフサイクル全体で再発する（P4 着手前の de-risk、P5 後の機能追加の実現性テスト）。本実装後にスパイクを `app/` 内でやると本実装が汚れるので、`mvp/` が P2 throwaway を隔離したのと同じ理屈で `poc/` に隔離する。
- 3 作業場: 使い捨て製品＝`mvp/`（P2）/ 使い捨て技術検証＝`poc/`（any phase）/ 本物＝`app/`（P5+）。prefix も `[mvp]`/`[poc]`/`[impl]` で 3 分。
- PoC のコードは throwaway（`poc/`）、結論・学びは `research/` か spec の `validation.md` に promote（コードは残さなくてよい）。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: GUIDELINES から手順再掲を削り正準リファレンスに圧縮）

### やったこと

- `02-GUIDELINES` からスキルが既に持つ手順再掲を削除: チェックポイントの「自動実行の境界」リスト（→`/checkpoint`）、セッション終了時チェックリスト（→`/wrapup-project` ポインタ化）、実装ルール 5 項→2 項に圧縮、スキル使い分けの説明 3 行→1 段落。正味 −19 行（146→127）。
- 正準リファレンス（記録先マップ・2層決定・commit prefix 表・status+pin・レビューゲート方針）は全て残置。

### 決めたこと

- GUIDELINES = 正準ポリシー/リファレンス（表）、スキル = 手順、の分担を徹底。重複は「手順再掲を消してスキルへのポインタ化」で解消し、表はスキルに移さない（移すと単一の正が断片化＝元の不満を再生産する）。
- 実際に削れたのは ~19 行のみ。残り ~127 行はほぼ irreducible な正準内容（表）。これ以上の短縮は正準を削ることになり断片化リスクなので止めた。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: MVP 期の技術メモ置き場を是正・mvp-design/technical.md 新設）

### やったこと

- フェーズ入替の残りカス「探索/MVP 期は `app-design/technical.md` に draft」を是正。**P2 の使い捨て技術構成は `docs/mvp-design/technical.md`（新設）、本実装スタックの検討は P3 の `app-design/technical.md`** に分離。
- `mvp-design/technical.md` 新設（最小: 種別・言語・FW・起動。「設計は書かない・使い捨て」明記）。`mvp-design/_index` に追加。
- 是正箇所: `04-ARCHITECTURE`（pin 前 bullet）/ `catchup` / `checkpoint` の "app-design/technical.md で draft" を撤去。MVP stack の記録先を `mvp/README`（docs 外）→ `docs/mvp-design/technical.md` に変更（ROADMAP P1 ゲート・`05-PROGRESS`・`mvp/README`）。
- 復元: `docs/progress/2026-W21.md` が作業ツリーから削除されていた（未コミット）ため HEAD から復元。

### 決めたこと

- `app-design/`（technical.md 含む）は P3 ワークスペース。**P2 でそこに書くのはフェーズ境界違反**（MVP/本実装の分離原則に反する。ユーザ指摘）。P2 の技術メモは `mvp-design/technical.md` へ。
- MVP 技術構成の記録先を `mvp/README`（docs 外）→ `docs/mvp-design/technical.md` に移す。理由: 「知識を docs 外に散らさない」原則 ＋「今このプロトタイプが何で動いてるか」を一望できる docs 内の home が要る。設計は書かず使い捨て前提で肥大化を防ぐ。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。

## 2026-05-25（追記: 「スタックに従う」を作業場ごとに是正）

### やったこと

- `/work` step3 と `02-GUIDELINES` 実装ルールの「スタックに従う（…探索/MVP 期は progress 一行）」を**作業場ごと**に修正: 本実装(`app/`)→ `04-ARCHITECTURE` pin 済み表（逸脱・追加は ADR 先行）/ MVP(`mvp/`)→ `mvp-design/technical.md`（使い捨て・ADR 不要）。
- 復元(2回目): `docs/progress/2026-W21.md` が再び作業ツリーから削除されていたため HEAD から復元。

### 決めたこと

- 「スタックに従う」は従う先が作業場で変わる（app/ は `04-ARCHITECTURE`、mvp/ は `mvp-design/technical.md`）。「探索/MVP 期は progress 一行」は決定記録(Tier1)の話なのでこの文脈から外した。理由: MVP は 04-ARCHITECTURE 表を使わない（mvp-design/technical.md に従う）ため「表を MVP 期に progress 一行で選定」は矛盾（ユーザ指摘）。

### 次やること

- （継続）公開前の英語 README 要否、LICENSE 名義確定。
- `docs/progress/2026-W21.md` の削除が 2 回続いた。外部要因（同期/エディタ等）の可能性。原因不明のため要観察。
